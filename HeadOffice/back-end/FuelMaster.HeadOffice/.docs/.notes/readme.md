# Edit sensitive values manually

If user wants to update some sensitive values such as (NozzleVolume,NozzleAmount), You should create UserActivity to see every action and make user able to roll back
